{
  "name": "skinca-ai-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix apps/consumer-mobile install --include=dev && npm --prefix apps/consumer-mobile run build"
  }
}
