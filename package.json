{
  "name": "skinca-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Skinca AI — Personalized Skincare Mobile App",
  "scripts": {
    "build": "cd apps/consumer-mobile && npm install && npx vite build",
    "dev": "cd apps/consumer-mobile && npm run dev"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
