import { SkincareWidgetElement } from '../widget-element';

describe('Storefront Widget Web Component Tests', () => {
  it('SkincareWidgetElement custom element should be defined', () => {
    expect(SkincareWidgetElement).toBeDefined();
  });
});
