import React from 'react';
import App from '../App';

describe('Consumer Web Portal Application Tests', () => {
  it('App component should render without crashing', () => {
    expect(App).toBeDefined();
  });
});
